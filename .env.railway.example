# Backend Environment Variables for Railway
# Copy these to Railway Dashboard → Your Service → Variables

# MongoDB Configuration (get from Railway MongoDB plugin)
MONGODB_URL=mongodb://mongo:password@mongodb.railway.internal:27017
DB_NAME=genai_crud

# AI Configuration (get from https://console.groq.com)
GROQ_API_KEY=gsk_your_groq_api_key_here

# ChromaDB Configuration (connect to ChromaDB service)
# Use the internal Railway URL for the Chroma service
CHROMA_HOST_ADDR=chroma.railway.internal
CHROMA_HOST_PORT=8000

# Port (automatically set by Railway, don't set manually)
# PORT=8000
